(ns example-rest-api.client-test
  (:require [clojure.test :refer :all]
            [ring.mock.request :as mock]
            [example-rest-api.const :as const]
            [example-rest-api.handler :as handler]))

